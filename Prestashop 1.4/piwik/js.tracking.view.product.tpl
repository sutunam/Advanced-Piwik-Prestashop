<script type="text/javascript">
	_paq.push([
		'setEcommerceView',
		'{$product_arr.id}', // (required) SKU: Product unique identifier
		'{$product_arr.name}', // (optional) Product name
		{$product_arr.category}, // (optional) Product category, or array of up to 5 categories
		{$product_arr.price} // (optional) Product Price as displayed on the page
	]);
</script>